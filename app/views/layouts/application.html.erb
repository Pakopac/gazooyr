<!DOCTYPE html>
<html>
  <head>
    <title>Gazooyr</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  You're on Gazooyr !
  <nav class="navbar navbar-default">
    <div class="container ">
      <div class="navbar-header">
      <ul class="nav navbar-nav">
        <li><a class="navbar-brand" href="#">Gazooyr</a></li>
        <li><a class="icon-bar" href="gazooies#index">Stream</a></li>
        <li><a href="#">Actualites</a></li>
        <li><a href="#">About</a></li>
        <li class="navbar-right"><a href="#">Gazooyer</a></li>
        <li> <%= link_to "S'enregistrer", '/users/sign_up' %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" id="account-dropdown" class="dropdown-toggle" data-toggle="dropdown">account-dropdown</a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><%= link_to "Mon Compte", '/users/edit' %></li>
            </ul>
          </li>
          <li><a href="#">Moi</a></li>
          <li>
            <%= link_to('logout', destroy_user_session_path, method: :delete) %>

          </li>
        <% else %>
          <li>
            <%= link_to('Sign in', new_user_session_path) %>
          </li>
        <% end %>
      </ul>
      </div>
    </div>
  </nav>
  <% if user_signed_in? %>
  Connecté
  Bonjour <%= current_user.email %>
  <% end %>



  <%= yield %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  </body>
</html>
